# @package _global_
#package global is neccessary!
defaults:
  - biofoundation/embedding/BEANS/embedding_config.yaml

tags: ["BEANS", "embeddings", "perch", "multiclass"]
seed: 2
  
trainer:
  devices: [1]
  min_epochs: 1
  max_epochs: 20

datamodule:
  k_samples: 0  