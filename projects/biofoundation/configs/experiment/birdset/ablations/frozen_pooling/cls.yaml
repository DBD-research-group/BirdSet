# @package _global_
#package global is neccessary!
defaults:
  - birdset/ablations/frozen_pooling/config.yaml 
  - override /module/network: beats.yaml 

module:
  network:
    model:
      pooling: "just_cls"