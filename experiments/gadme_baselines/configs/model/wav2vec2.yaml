name: wav2vec2
name_hf: facebook/wav2vec2-base-960h

optimizer:
  _target_: torch.optim.AdamW
  lr: 5e-5
  weight_decay: 0.01

scheduler:
  scheduler:
    _target_: transformers.get_linear_schedule_with_warmup
  extras:
    warmup_ratio: 0.05

trainer:
  n_epochs: 5
  batch_size: 12
