name: ast
name_hf: MIT/ast-finetuned-audioset-10-10-0.4593

optimizer:
  _target_: torch.optim.AdamW
  lr: 5e-5
  weight_decay: 0.01

scheduler:
  scheduler:
    _target_: transformers.get_linear_schedule_with_warmup
  extras:
    warmup_ratio: 0.05

trainer:
  n_epochs: 100
  batch_size: 8