# @package _global_
#package global is neccessary!
defaults:
  - biofoundation/birdset/linearprobing/linearprobing_config.yaml 
  - override /module/network: perch_logreg.yaml  
tags: ["biofoundation", "perch", "linearprobing", "augs_birdset"]

logger:
  wandb:
    tags: ${tags}
    group: "biofoundation_perch_linearprobing_BirdSet__${datamodule.dataset.dataset_name}"
    mode: online
    version: perch_linearprobing_BirdSet_${datamodule.dataset.dataset_name}_${seed}_${start_time} #to resume train run # needs to be tested