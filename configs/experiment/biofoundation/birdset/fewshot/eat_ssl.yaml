# @package _global_
#package global is neccessary!
defaults:
  - biofoundation/birdset/fewshot/fewshot_config.yaml 
  - override /module/network: eat.yaml


tags: ["biofoundation", "eat_ssl", "fewshot", "k32"]

trainer:
  min_epochs: 1
  max_epochs: 2
  devices: [0]

logger:
  wandb:
    tags: ${tags}
    group: "biofoundation_eat_ssl_fewshot_BirdSet__${datamodule.dataset.dataset_name}"
    mode: online
    version: eat_ssl_fewshot_BirdSet_${datamodule.dataset.dataset_name}_${seed}_${start_time} #to resume train run # needs to be tested