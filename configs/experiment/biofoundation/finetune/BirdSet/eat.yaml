# @package _global_
#package global is neccessary!
defaults:
  - biofoundation/finetune/BirdSet/BirdSet_config.yaml
  - override /module/embedding_model: ../../module/network/eat_soundnet.yaml

module:
  embedding_model:
      model:
        num_classes: 10 # Just random value for now as its not needed 