# @package _global_
#package global is neccessary!
defaults:
  - override /datamodule: /inference/UHH_inf.yaml
  - override /module: multilabel.yaml
  - override /module/network: eat_soundnet.yaml
  - override /trainer: single_gpu.yaml
  - override /datamodule/transforms: inference_default.yaml
  - override /paths: default.yaml
  - override /hydra: default.yaml

#override for inference
# datamodule:
#   _target_: birdset.datamodule.inference_datamodule.InferenceDataModule

tags: ["UHH", "eat", "multilabel", "inference"]
seed: 2
ckpt_path: /workspace/logs/SavedModels/2024-04-17_131106/callback_checkpoints/eat_XCM_125.ckpt

test: True

logger:
  wandb:
    tags: ${tags}
    group: "UHH_inference_eat_xcm"
    mode: online
    version: pretrain_xcm_eat_${seed}_${start_time} #to resume train run # needs to be tested
    #version: pretraineatxcm_2 #to resume train run # needs to be tested

module:
  network:
    torch_compile: False
    model:
      #local_checkpoint: /home/lrauch/mnt/train/runs/XCM/ast/2024-04-03_133040/callback_checkpoints/last.ckpt
      local_checkpoint : /workspace/logs/SavedModels/2024-04-17_131106/callback_checkpoints/eat_XCM_125.ckpt
      pretrain_info:
        hf_path: DBD-research-group/BirdSet
        hf_name: UHH
        hf_pretrain_name: XCM

trainer:
  min_epochs: 1
  max_epochs: 1
  devices: [0]

datamodule:
  transforms:
    preprocessing:
        normalize_waveform: instance_normalization

  loaders:
    test:
      batch_size: 64
      num_workers: 24
      shuffle: False

