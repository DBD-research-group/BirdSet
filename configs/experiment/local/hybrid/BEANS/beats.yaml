# @package _global_
#package global is neccessary!
defaults:
  - local/hybrid/BEANS/hybrid_config.yaml 
  - override /module/embedding_model: ../../module/network/beats_logreg.yaml  

tags: ["BEANS", "hybrid", "beats", "multiclass"]
seed: 2

trainer:
  devices: [1]
  min_epochs: 1
  max_epochs: 5
  precision: 32

module:
  ft_max_epochs: 5
  ft_lr: 1e-4
  


