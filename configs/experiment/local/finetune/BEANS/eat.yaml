# @package _global_
#package global is neccessary!
defaults:
  - local/finetune/BEANS/finetune_config.yaml 
  - override /module/embedding_model: ../../module/network/eat_m_soundnet.yaml 

tags: ["BEANS", "finetune", "eat", "multiclass"]
seed: 2

trainer:
  devices: [1]
  min_epochs: 1
  

